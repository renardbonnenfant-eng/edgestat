{
  "name": "edgestat",
  "version": "1.0.0",
  "description": "EdgeStat — analyse statistique de paris sportifs",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/src/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
